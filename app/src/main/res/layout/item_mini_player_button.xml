<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_marginBottom="16dp"
    android:background="@drawable/button_shadow_style"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    android:minHeight="80dp"
    android:paddingVertical="12dp"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:clipChildren="false">

    <!-- Color bar placeholder -->
    <View
        android:id="@+id/colorBar"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

    <!-- Line 1: Source Info (Transport + Format + Bitrate) -->
    <TextView
        android:id="@+id/connectionInfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Origine: Loading..."
        android:textSize="11sp"
        android:textColor="#A8A8A8"
        android:singleLine="true"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:scrollHorizontally="true"
        android:layout_marginBottom="2dp" />

    <!-- Line 2: Output Info (Device + Format + Mixer + Offload) -->
    <TextView
        android:id="@+id/qualityInfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Uscita: Loading..."
        android:textSize="11sp"
        android:textColor="#7C7C7C"
        android:singleLine="true"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:scrollHorizontally="true"
        android:layout_marginBottom="4dp" />

    <!-- Line 3: Radio Station Name or Track Title (depending on source) -->
    <TextView
        android:id="@+id/trackInfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Radio Station Name / Track Title"
        android:textSize="13sp"
        android:textColor="#FFFFFF"
        android:singleLine="true"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:scrollHorizontally="true"
        android:layout_marginBottom="2dp" />

    <!-- Line 4: Artist ➤ Title (for web radio) -->
    <TextView
        android:id="@+id/extraInfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text=""
        android:textSize="11sp"
        android:textColor="#BBBBBB"
        android:singleLine="true"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:scrollHorizontally="true"
        android:visibility="gone" />

</LinearLayout>
